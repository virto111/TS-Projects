type Drink = [string, boolean, number];

//* Tuples - a very specific record (with order)
const drink: Drink = ['kola', true, 0.12];
const sprite: Drink = ['sprite', true, 0.12];

//! csv - we might use Tuples, BUT usually, not big usage :-), we have Object Literals!